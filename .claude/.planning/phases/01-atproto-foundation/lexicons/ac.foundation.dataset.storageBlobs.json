{
  "lexicon": 1,
  "id": "science.alt.dataset.storageBlobs",
  "defs": {
    "main": {
      "type": "object",
      "description": "Storage via ATProto PDS blobs for WebDataset tar archives. Each blob contains one or more tar files. Used in science.alt.dataset.entry storage union for maximum decentralization.",
      "required": [
        "blobs"
      ],
      "properties": {
        "blobs": {
          "type": "array",
          "description": "Array of blob references for WebDataset tar files",
          "items": {
            "type": "blob",
            "description": "Blob reference to a WebDataset tar archive"
          },
          "minLength": 1
        }
      }
    }
  }
}
