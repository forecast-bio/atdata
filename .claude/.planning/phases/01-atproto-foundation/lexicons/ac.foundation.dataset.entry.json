{
  "lexicon": 1,
  "id": "science.alt.dataset.entry",
  "defs": {
    "main": {
      "type": "record",
      "description": "Index record for a WebDataset-backed dataset with references to storage location and sample schema",
      "key": "tid",
      "record": {
        "type": "object",
        "required": [
          "name",
          "schemaRef",
          "storage",
          "createdAt"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable dataset name",
            "maxLength": 200
          },
          "schemaRef": {
            "type": "string",
            "format": "at-uri",
            "description": "AT-URI reference to the schema record for this dataset's samples",
            "maxLength": 500
          },
          "storage": {
            "type": "union",
            "description": "Storage location for dataset files (WebDataset tar archives)",
            "refs": [
              "science.alt.dataset.storageExternal",
              "science.alt.dataset.storageBlobs"
            ]
          },
          "description": {
            "type": "string",
            "description": "Human-readable description of the dataset",
            "maxLength": 5000
          },
          "metadata": {
            "type": "ref",
            "ref": "#datasetMetadata",
            "description": "Structured metadata for the dataset. Provides well-known fields for common metadata plus a custom object for domain-specific extensions."
          },
          "tags": {
            "type": "array",
            "description": "Searchable tags for dataset discovery. Aligns with Schema.org keywords property.",
            "items": {
              "type": "string",
              "maxLength": 150
            },
            "maxLength": 30
          },
          "size": {
            "type": "ref",
            "ref": "#datasetSize",
            "description": "Dataset size information (optional)"
          },
          "license": {
            "type": "string",
            "description": "License identifier or URL. SPDX identifiers recommended (e.g., MIT, Apache-2.0, CC-BY-4.0) or full SPDX URLs (e.g., http://spdx.org/licenses/MIT). Aligns with Schema.org license property.",
            "maxLength": 200
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Timestamp when this dataset record was created"
          }
        }
      }
    },
    "datasetMetadata": {
      "type": "object",
      "description": "Structured metadata for a dataset record. Well-known fields are first-class properties visible to ATProto relays and indexers. The custom object provides extensibility for domain-specific metadata.",
      "properties": {
        "sourceUri": {
          "type": "string",
          "description": "URI or identifier of the upstream data source",
          "maxLength": 1000
        },
        "createdBy": {
          "type": "string",
          "description": "Tool, pipeline, or user that created this dataset",
          "maxLength": 500
        },
        "version": {
          "type": "string",
          "description": "Dataset version string (e.g., '2.1.0')",
          "maxLength": 100
        },
        "processingSteps": {
          "type": "array",
          "description": "Ordered list of processing/transformation steps applied",
          "items": {
            "type": "string",
            "maxLength": 200
          },
          "maxLength": 50
        },
        "split": {
          "type": "string",
          "description": "Dataset split name (e.g., 'train', 'test', 'validation')",
          "maxLength": 100
        },
        "custom": {
          "type": "object",
          "description": "Arbitrary domain-specific metadata key-value pairs",
          "maxProperties": 100
        }
      }
    },
    "datasetSize": {
      "type": "object",
      "description": "Information about dataset size",
      "properties": {
        "samples": {
          "type": "integer",
          "description": "Total number of samples in the dataset",
          "minimum": 0
        },
        "bytes": {
          "type": "integer",
          "description": "Total size in bytes",
          "minimum": 0
        },
        "shards": {
          "type": "integer",
          "description": "Number of WebDataset shards",
          "minimum": 1
        }
      }
    }
  }
}
