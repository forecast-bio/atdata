# Issue #201: Create live test fixtures and conftest setup

## Verdict: CLOSE

## Evidence
`tests/integration/conftest.py` (173 lines) provides the complete fixture infrastructure for live integration tests. Session-scoped fixtures for ATProto credentials, MinIO credentials, PostgreSQL DSN, and Redis URL all use environment variable skip logic (calling `pytest.skip()` when credentials are not configured). A per-run UUID prefix (`RUN_ID`) isolates all resources created during each test run. Cleanup teardowns are implemented for MinIO (deletes all objects then the bucket), PostgreSQL (truncates tables with RESTART IDENTITY CASCADE), and Redis (flushes the test database). The `pytest_collection_modifyitems` hook auto-marks every test under `tests/integration/` with the `integration` marker.

## Codebase References
- `tests/integration/conftest.py` line 35 -- `RUN_ID` per-run UUID prefix for resource isolation
- `tests/integration/conftest.py` lines 26-31 -- `pytest_collection_modifyitems` hook auto-marking integration tests
- `tests/integration/conftest.py` lines 42-52 -- `atproto_credentials` session fixture with env-var skip
- `tests/integration/conftest.py` lines 55-62 -- `atproto_client` session fixture with `Atmosphere.login()`
- `tests/integration/conftest.py` lines 68-83 -- `minio_credentials` session fixture with env-var skip
- `tests/integration/conftest.py` lines 86-106 -- `minio_bucket` fixture with teardown (delete objects + bucket)
- `tests/integration/conftest.py` lines 112-118 -- `postgres_dsn` fixture with env-var skip
- `tests/integration/conftest.py` lines 121-137 -- `postgres_provider` fixture with TRUNCATE teardown
- `tests/integration/conftest.py` lines 143-149 -- `redis_url` fixture with env-var skip
- `tests/integration/conftest.py` lines 152-165 -- `redis_provider` fixture with `flushdb()` teardown
- `tests/integration/conftest.py` lines 171-173 -- `unique_name()` helper combining prefix + RUN_ID + random UUID
