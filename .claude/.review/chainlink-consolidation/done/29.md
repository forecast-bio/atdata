# Issue #29: Implement Lens transformation publishing

## Verdict: CLOSE

## Evidence
`LensPublisher` and `LensLoader` are fully implemented. `LensPublisher.publish()` creates `science.alt.dataset.lens` records with required `getterCode` and `putterCode` fields pointing to external git repositories (repository URL, commit hash, function path). `publish_from_lens()` extracts getter/putter function names and module paths from existing `Lens` objects. `LensLoader` provides get, typed get, list, and `find_by_schemas()` for discovering lenses that transform between specific schema types. The security model stores code as external references only -- no inline code execution from ATProto records.

## Codebase References
- `src/atdata/atmosphere/lens.py` lines 22-125 -- `LensPublisher` class with `publish()` method
- `src/atdata/atmosphere/lens.py` lines 60-125 -- `publish()` creates lens record with `LexCodeReference` objects
- `src/atdata/atmosphere/lens.py` lines 98-107 -- `getter_code` and `putter_code` construction
- `src/atdata/atmosphere/lens.py` lines 127-182 -- `publish_from_lens()` extracts function names from Lens objects
- `src/atdata/atmosphere/lens.py` lines 185-289 -- `LensLoader` class
- `src/atdata/atmosphere/lens.py` lines 210-231 -- `get()` validates `$type` matches `science.alt.dataset.lens`
- `src/atdata/atmosphere/lens.py` lines 261-288 -- `find_by_schemas()` filters lenses by source/target schema URIs
- `src/atdata/atmosphere/_lexicon_types.py` lines 442-481 -- `LexCodeReference` dataclass
