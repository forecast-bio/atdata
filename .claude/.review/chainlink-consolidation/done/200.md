# Issue #200: Add live ATProto network integration tests

## Verdict: CLOSE

## Evidence
Full live integration test suite implemented in `tests/integration/test_atproto_live.py` (383 lines) covering authentication (login, session export/reimport, invalid credentials), blob operations (upload/download, larger blobs), record CRUD (create/get/delete, list), schema publishing (publish/get, NDArray fields), dataset publishing (publish with URLs), blob round-trip (write samples, upload as blob, retrieve and iterate), error handling (unauth publish, nonexistent record, E2E with local fixture), and cleanup (sweep schemas and datasets by RUN_ID prefix). CI workflow defined in `.github/workflows/integration.yml` (113 lines) with GitHub Actions service containers for PostgreSQL 16, Redis 7, and MinIO, running on push to main/develop, PRs to main, nightly cron, and manual dispatch. All 5 sub-issues (#201-#205) are implemented and ready to close.

## Codebase References
- `tests/integration/test_atproto_live.py` -- full test file (383 lines)
  - Lines 68-87: `TestAuthentication` class (3 tests: login, session export/reimport, invalid credentials)
  - Lines 93-128: `TestBlobOperations` class (2 tests: upload/download, larger blob)
  - Lines 133-158: `TestRecordCRUD` class (2 tests: create/get/delete, list records)
  - Lines 164-212: `TestSchemaPublishing` class (2 tests: publish/get, NDArray fields)
  - Lines 218-248: `TestDatasetPublishing` class (1 test: publish with URLs)
  - Lines 254-306: `TestBlobRoundTrip` class (1 test: write/upload/iterate E2E)
  - Lines 312-370: `TestErrorHandling` class (3 tests: unauth, nonexistent, E2E fixture)
  - Lines 376-383: `TestZZZCleanup` class (2 tests: cleanup schemas, cleanup datasets)
  - Lines 51-62: `_cleanup_records()` helper function
- `tests/integration/conftest.py` -- integration fixtures (173 lines)
- `tests/integration/test_s3_live.py` -- S3 live tests (217 lines)
- `.github/workflows/integration.yml` -- CI workflow (113 lines)
  - Lines 8-10: nightly cron schedule + workflow_dispatch trigger
  - Lines 28-51: PostgreSQL + Redis service containers
  - Lines 70-80: MinIO container startup
  - Lines 82-105: test execution with all credential env vars
