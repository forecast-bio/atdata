# Issue #558: Rename CLI: atdata local -> atdata infra

**Priority:** medium
**Parent:** #557
**Status:** open -> CLOSED
**Created:** 2026-02-02

## History

Sub-issue of #557. The original title says "atdata infra" but the chainlink issue title says "atdata local -> atdata infra". This tracks the CLI subcommand rename specifically.

## Current State Assessment

The rename is complete:

- `src/atdata/cli/__init__.py` line 33-38: `infra_app = typer.Typer(name="infra", help="Manage development infrastructure.", ...)` registered as `app.add_typer(infra_app, name="infra")`.
- `src/atdata/cli/infra.py` contains the implementation (`local_up`, `local_down`, `local_status`).
- The docstring at the top of `cli/__init__.py` documents `atdata infra up/down/status`.
- No references to `atdata local` remain in the CLI module.

## Decision

**CLOSE** -- CLI subcommand renamed from `local` to `infra`.
