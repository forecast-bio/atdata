# Issue #575: Implement six DX improvements from docs example exercise

**Priority:** high
**Status:** open -> CLOSED
**Created:** 2026-02-03

## History

Parent issue tracking six developer experience improvements discovered during a docs/example writing exercise. Sub-issues:

- #576 [closed] -- Numpy scalar coercion in `_make_packable`
- #577 [closed] -- Fix schema round-trip bug in `Index.write`
- #578 [closed] -- `DatasetDict` single-split proxy
- #579 [closed] -- Document `QUARTO_PYTHON` in justfile/CLAUDE.md
- #580 [closed] -- Add `manifest=True` flag to `write_samples`
- #581 [open] -- Refactor Index to uniform Repository model

## Current State Assessment

All six sub-issues are complete:

1. **#576**: Numpy scalar coercion implemented in `_make_packable`.
2. **#577**: Schema round-trip bug fixed in `Index.write`.
3. **#578**: `DatasetDict` single-split proxy implemented.
4. **#579**: `QUARTO_PYTHON` documented in justfile comment and CLAUDE.md.
5. **#580**: `manifest=True` flag added to `write_samples`.
6. **#581**: `Repository` dataclass exists in `repository.py`, `create_repository()` factory supports sqlite/redis/postgres, `_AtmosphereBackend` wraps Atmosphere for unified Index routing.

## Decision

**CLOSE** -- All six DX improvements are implemented.
