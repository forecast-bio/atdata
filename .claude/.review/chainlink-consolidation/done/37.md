# Issue #37: Implement Python code generator from schema records

## Verdict: CLOSE

## Evidence
Fully implemented in `_schema_codec.py` and `_stub_manager.py`. The
`schema_to_type()` function dynamically generates `PackableSample` subclasses
from JSON Schema definitions at runtime. The `generate_stub()` function produces
`.pyi` type stub source for IDE autocompletion. The `generate_module()` function
produces complete `.py` module source files. The `StubManager` class manages stub
file generation and caching for IDE integration. Together these provide complete
Python code generation from ATProto schema records.

## Codebase References
- `src/atdata/_schema_codec.py` line 234: `def schema_to_type()` -- dynamically generates `PackableSample` subclass from JSON Schema dict
- `src/atdata/_schema_codec.py` line 380: `def generate_stub(schema: dict) -> str` -- generates `.pyi` stub source
- `src/atdata/_schema_codec.py` line 457: `def generate_module(schema: dict) -> str` -- generates full `.py` module source
- `src/atdata/_schema_codec.py` line 38: `_type_cache` -- caches generated types with LRU eviction
- `src/atdata/_stub_manager.py` line 74: `class StubManager` -- manages stub file generation and caching for IDE support
