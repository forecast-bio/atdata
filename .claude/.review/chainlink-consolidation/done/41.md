# Issue #41: Write integration tests for full publish/discover/load cycle

## Verdict: CLOSE

## Evidence
Integration tests cover the full publish/discover/load cycle across multiple
backends. The `test_atproto_live.py` file (383 lines) tests live ATProto
operations including login, blob upload/download, schema publishing, dataset
publishing with URLs, write-upload-iterate, and full end-to-end with local
fixtures. The `test_workflow_atmosphere.py` file (640 lines) tests atmosphere
workflows with mocked clients including publish-schema-publish-dataset,
session management, blob store operations, and cross-backend scenarios.
`test_workflow_cross_backend.py` (568 lines) tests interactions between local
and atmosphere backends. Additional integration tests exist for Redis, Postgres,
and S3 backends.

## Codebase References
- `tests/integration/test_atproto_live.py` (383 lines) -- live ATProto integration tests
  - Line 167: `test_publish_and_get_schema` -- schema publish/load cycle
  - Line 221: `test_publish_dataset_with_urls` -- dataset publish with URL storage
  - Line 257: `test_write_upload_iterate` -- write, upload blobs, iterate cycle
  - Line 329: `test_full_e2e_with_local_fixture` -- full end-to-end with local data
- `tests/test_workflow_atmosphere.py` (640 lines) -- atmosphere workflow tests
  - Line 85: `test_login_publish_schema_publish_dataset` -- full publish cycle
  - Line 506: `test_write_shards_uploads_blobs` -- blob upload workflow
- `tests/test_workflow_cross_backend.py` (568 lines) -- cross-backend interaction tests
- `tests/integration/test_redis_live.py` -- live Redis integration tests
- `tests/integration/test_postgres_live.py` -- live PostgreSQL integration tests
- `tests/integration/test_s3_live.py` -- live S3 integration tests
