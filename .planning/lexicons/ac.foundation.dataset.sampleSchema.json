{
  "lexicon": 1,
  "id": "ac.foundation.dataset.sampleSchema",
  "defs": {
    "main": {
      "type": "record",
      "description": "Definition of a PackableSample-compatible sample type. Supports versioning via rkey format: {NSID}@{semver}. Schema format is extensible via union type.",
      "key": "any",
      "record": {
        "type": "object",
        "required": [
          "name",
          "version",
          "schemaType",
          "schema",
          "createdAt"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Human-readable display name for this sample type. Used for documentation and UI. The NSID in the record URI provides unique identification; name collisions across NSIDs are acceptable.",
            "maxLength": 100
          },
          "version": {
            "type": "string",
            "description": "Semantic version (e.g., '1.0.0')",
            "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$",
            "maxLength": 100
          },
          "schemaType": {
            "type": "ref",
            "ref": "ac.foundation.dataset.schemaType",
            "description": "Type of schema definition. This field indicates which union member is present in the schema field."
          },
          "schema": {
            "type": "union",
            "refs": ["ac.foundation.dataset.sampleSchema#jsonSchemaFormat"],
            "closed": false,
            "description": "Schema definition for this sample type. Currently supports JSON Schema Draft 7. Union allows for future schema formats (Avro, Protobuf, etc.) without breaking changes."
          },
          "description": {
            "type": "string",
            "description": "Human-readable description of what this sample type represents",
            "maxLength": 5000
          },
          "metadata": {
            "type": "object",
            "description": "Optional metadata about this schema. Common fields include author, license, and tags, but any additional fields are permitted.",
            "maxProperties": 50,
            "properties": {
              "author": {
                "type": "string",
                "description": "Creator of this schema (DID, handle, or name)",
                "maxLength": 200
              },
              "license": {
                "type": "string",
                "description": "License identifier (e.g., MIT, Apache-2.0, CC-BY-4.0)",
                "maxLength": 100
              },
              "tags": {
                "type": "array",
                "description": "Categorization tags for discovery",
                "items": {
                  "type": "string",
                  "maxLength": 50
                },
                "maxLength": 20
              }
            }
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Timestamp when this schema version was created. Immutable once set (ATProto records are permanent)."
          }
        }
      }
    },
    "jsonSchemaFormat": {
      "type": "object",
      "description": "JSON Schema Draft 7 format for sample type definitions. Used with NDArray shim for array types.",
      "required": ["$type", "$schema", "type", "properties"],
      "properties": {
        "$type": {
          "type": "string",
          "const": "ac.foundation.dataset.sampleSchema#jsonSchemaFormat"
        },
        "$schema": {
          "type": "string",
          "const": "http://json-schema.org/draft-07/schema#",
          "description": "JSON Schema version identifier"
        },
        "type": {
          "type": "string",
          "const": "object",
          "description": "Sample types must be objects"
        },
        "properties": {
          "type": "object",
          "description": "Field definitions for the sample type",
          "minProperties": 1
        },
        "ndarrayShimUri": {
          "type": "string",
          "format": "uri",
          "description": "URI to the NDArray JSON Schema shim definition. Optional, defaults to https://foundation.ac/schemas/atdata-ndarray-bytes/1.0.0",
          "maxLength": 500
        }
      }
    }
  }
}
